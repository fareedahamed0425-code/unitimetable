{
  "name": "university-timetable-system",
  "version": "1.0.0",
  "description": "Production-grade University Timetable Management and Automatic Generation Platform with FET Capability Replication and Smart Preferences",
  "scripts": {
    "dev:backend": "cd backend && npx tsx watch src/server.ts",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "npx concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:backend": "cd backend && npx tsc",
    "build:frontend": "cd frontend && npm run build",
    "build": "npm run build:backend && npm run build:frontend",
    "test:backend": "cd backend && npx tsx src/tests/test_all.ts",
    "test": "npm run test:backend"
  },
  "keywords": [
    "university-timetabling",
    "fet",
    "constraint-satisfaction",
    "heuristics",
    "simulated-annealing",
    "academic-management"
  ],
  "author": "Google DeepMind Advanced Agentic Coding",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
